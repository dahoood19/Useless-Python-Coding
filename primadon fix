task.wait(0.5) local Players=game:GetService("Players") local localPlayer=Players.LocalPlayer local myChar=localPlayer.Character local target=nil for _,v in ipairs(workspace:GetChildren()) do if v:IsA("Model") and v~=myChar and v.Name==localPlayer.Name and not v:FindFirstChild("MonkyEvent") then target=v break end end if target then local running=true task.spawn(function() local deadline=tick()+2 while running and tick()<deadline do pcall(function() for _,p in ipairs(target:GetDescendants()) do if p:IsA("BasePart") then p.Anchored=true p.CanCollide=false p.Velocity=Vector3.new(0,0,0) p.RotVelocity=Vector3.new(0,0,0) p.AssemblyLinearVelocity=Vector3.new(0,0,0) p.AssemblyAngularVelocity=Vector3.new(0,0,0) end end local hum=target:FindFirstChildOfClass("Humanoid") if hum then hum.MaxHealth=0 hum.Health=0 hum:TakeDamage(99999) end end) task.wait() end end) task.wait(0.5) pcall(function() local hrp=target:FindFirstChild("HumanoidRootPart") if hrp then hrp:Destroy() end end) pcall(function() local hum=target:FindFirstChildOfClass("Humanoid") if hum then hum:Destroy() end end) pcall(function() target:Destroy() end) running=false task.delay(0.5,function() pcall(function() if target and target.Parent then target:Destroy() end end) end) task.delay(1.5,function() pcall(function() if target and target.Parent then target:Destroy() end end) end) else for _,v in ipairs(workspace:GetChildren()) do if v:IsA("Model") and v~=myChar and not v:FindFirstChild("MonkyEvent") then local hum=v:FindFirstChildOfClass("Humanoid") if hum then pcall(function() hum.MaxHealth=0 hum.Health=0 hum:TakeDamage(99999) local hrp=v:FindFirstChild("HumanoidRootPart") if hrp then hrp:Destroy() end hum:Destroy() v:Destroy() end) break end end end end
